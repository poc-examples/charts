# {{ if .Values.devspaces.enabled }}
# apiVersion: argoproj.io/v1alpha1
# kind: ApplicationSet
# metadata:
#   name: devspaces-appset
#   namespace: "{{ .Values.gitops.namespace }}"
#   annotations:
#     argocd.argoproj.io/sync-wave: "-2"
# spec:
#   goTemplate: true
#   goTemplateOptions:
#     - "missingkey=error"
#   generators:
#     - clusters:
#         selector:
#           matchLabels:
#             cluster: default
#   template: 
#     metadata: 
#       name: devspaces-service
#     spec:
#       destination:
#         namespace: devspaces
#         server: https://kubernetes.default.svc
#       source:
#         chart: dev-spaces
#         targetRevision: "{{ .Values.devspaces.chartVersion }}"
#         repoURL: https://poc-examples.github.io/charts
#         helm:
#           parameters:
#             - name: domain
#               value: "{{ `{{ .metadata.labels.domain }}` }}"
#       project: system
#       syncPolicy:
#         retry:
#           limit: 10
#           backoff:
#             duration: 5s
#             factor: 2
#             maxDuration: 30s
#         automated:
#           prune: true
#           selfHeal: true
#         managedNamespaceMetadata:
#           labels:
#             argocd.argoproj.io/managed-by: "{{ .Values.gitops.namespace }}"
#         syncOptions:
#           - CreateNamespace=true
#           - Validate=false
# {{ end }}
